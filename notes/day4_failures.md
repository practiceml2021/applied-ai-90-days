What failed:
The external OpenAI API request failed due to invalid authentication credentials.

Why it failed:
The API key was intentionally misconfigured to simulate an external dependency failure.

What assumption was wrong:
That external services will always be available and correctly configured.

